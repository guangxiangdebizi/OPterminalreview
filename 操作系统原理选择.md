一、进程基本概念与状态
1.从下面对临界区的论述中，选出一条正确的论述。（）
A. 临界区是指进程中用于实现进程同步的那段代码
B. 临界区是指进程中用于访问共享资源的那段代码
C. 临界区是指进程中访问临界资源的那段代码
D. 临界区是指进程中用于实现进程通信的那段代码
2.并发性是指若干事件在（）发生。
A. 同一时刻
B. 不同时刻
C. 不同时间间隔内
D. 同一时间间隔内
3.当条件满足时，进程可以由阻塞态直接转换为运行态。（）
A. 对
B. 错
4.进程的基本状态（）可以由其他两种基本状态转变而来。
A. 执行状态
B. 就绪状态
C. 阻塞状态
D. 新建状态
5.进程并发地在处理机上执行时，（）。
A. 进程之间是无关的，系统是封闭的
B. 上述都不对
C. 进程之间具有交互性、并发性，它们既相互依赖，又相互制约
D. 进程之间可能具有交往，也可能无关
6.一个进程因为等待事件的发生而处于等待状态，当等待事件完成后，就用( )将其转换为就绪状态。
A. 撤销原语
B. 创建原语
C. 唤醒原语
D. 阻塞原语
7.进程的状态和优先级信息存放在（）。
A. 快表
B. 页表
C. PCB
D. JCB
8.下面所述步骤中，( )不是创建进程所需的步骤
A. 由CPU调度程序为进程调度CPU
B. 为进程分配内存
C. 建立一个进程控制块
D. 将进程控制块链入就绪队列
9.多道系统环境下，操作系统分配资源以（）为基本单位。
A. 进程
B. 指令
C. 作业
D. 程序
二、信号量与同步互斥
1.若记录型信号量S的初值为15，当前值为-15，则表示有（）等待进程。
A. 16个
B. 15个
C. 31个
D. 30个
2.在非剥夺调度方式下，运行进程执行V原语后，其状态（）。
A. 不变
B. 可能不变
C. 可能要变
D. 要变
3.若记录型信号量S的初值是3，则当前值为1时表示有（）个阻塞等待进程。
A. 2个
B. 3个
C. 1个
D. 0个
E. 1个或者2个
4.在9个生产者，5个消费者，共享容量为7的缓冲区的生产者-消费者问题中。互斥使用缓冲区的信号量S的初值为（）。
A. 1
B. 5
C. 9
D. 7
5.操作系统中有一组特殊的程序，它们不能被系统中断，在操作系统中称为（）。
A. 子程序
B. 控制模块
C. 原语
D. 初始化程序
三、调度算法
1.为了对紧急进程或重要进程进行调度，调度算法采用（）。
A. 优先级调度
B. 先进先出调度算法
C. 轮转法
D. 短进程优先调度
2.在分时系统中，时间片一定，（），响应时间越长。
A. 用户数越少
B. 用户数越多
C. 内存越少
D. 内存越多
3.一个作业8:00到达系统，估计运行时间为1小时。若10:00开始执行该作业，其响应比是（）。
A. 3
B. 1
C. 2
D. 0.5
4.进程调度是从( )中选择一个进程投入运行。
A. 就绪队列
B. 等待队列
C. 作业后备队列
D. 提交队列
四、死锁
1.系统中有4个进程都要使用某类资源。若每个进程最多需要3个该类资源，为保证系统不发生死锁，系统应提供该类资源至少是（）。
A. 4个
B. 12个
C. 9个
D. 3个
2.进程P1使用资源情况：申请资源S1，…申请资源S2，…释放资源S1；进程P2使用资源情况：申请资源S2，…申请资源S1，…释放资源S2，系统并发执行进程P1，P2，系统将（）。
A. 不会产生死锁
B. 必定产生死锁
C. 无法确定是否会产生死锁
D. 可能产生死锁
3.下面关于安全状态和非安全状态说法正确的是（）。
A. 安全状态是没有死锁的状态，非安全状态是有死锁的状态
B. 安全状态是没有死锁的状态，非安全状态可能有死锁的状态
C. 安全状态是可能没有死锁的状态，非安装状态有死锁的状态
D. 安全状态是可能有死锁的状态，非安全状态也可能有死锁状态
五、操作系统基础与中断
1.计算机操作系统的功能是（）。
A. 把源程序代码转换为目标代码
B. 完成计算机硬件与软件之间的转换
C. 控制、管理计算机系统的资源和程序的执行
D. 实现计算机用户之间的相互交流
2.在操作系统中采用多道程序设计方式提高CPU和外部设备的（）。
A. 可靠性
B. 稳定性
C. 兼容性
D. 利用效率
3.实时操作系统必须在（）内处理来自外部的事件。
A. 时间片
B. 被控制对象规定时间
C. 周转时间
D. 一个机器周期
4.中断处理和子程序调用都需要压栈保护现场，中断处理一定会保存而子程序调用不需要保存其内容的是（）。
A. 程序状态字寄存器
B. 通用数据寄存器
C. 程序计数器
D. 通用地址寄存器